<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Login Status</title>
</head>

<body>
  <h1>Login Status</h1>

  <p>You are
    <span>
      <% if (isAuthenticated) { %>
        Logged in!
        <% if (currentUser) { %>
          Welcome, <%= currentUser.username %>!
        <% } %>
      <% } else { %>
        NOT logged in.
      <% } %>
    </span>
  </p>

  <% if (isAuthenticated) { %>
    <a href="/logout">Logout</a>
  <% } else { %>
    <a href="/login">Login</a>
  <% } %>

</body>

</html>